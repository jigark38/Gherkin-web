import { UppertextDirective } from './uppertext.directive';

describe('UppertextDirective', () => {
  it('should create an instance', () => {
    const directive = new UppertextDirective();
    expect(directive).toBeTruthy();
  });
});
